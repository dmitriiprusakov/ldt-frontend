import React, { FC } from "react";

import css from "./index.module.css";

const RecentEvents: FC = () => {
	return (
		<section className={css.section}>
			<div className={css.content} />
		</section>
	);
};

export default RecentEvents;
